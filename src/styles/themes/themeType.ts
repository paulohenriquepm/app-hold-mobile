type ThemeType = {
  title: string;

  colors: {
    primary: string;
    secondary: string;
    danger: string;

    background: string;
    title: string;
    text: string;
    subText: string;
    placeholder: string;

    box: string;
    buttonText: string;
    buttonGreyBorderColor: string;
    themeSwitcher: string;
  };

  fonts: {
    regular: string;
    medium: string;
    bold: string;
  };
};

export { ThemeType };
